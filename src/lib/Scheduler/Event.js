const _ = require('lodash');
const SchedulerEvent = require('./models/event');

class Event {
  /**
   *
   * @param options Options Object
   * @param {Object|String} options.user User Object / User ID
   */
  static newEvent = (options) => {

  };
}